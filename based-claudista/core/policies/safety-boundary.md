# Safety Boundary Policy

Classify actions as `allow`, `warn`, `escalate`, or `block`.

Escalate before deployment, credential handling, destructive git, irreversible data movement, external account actions, protected-resource writes, or unattended mutation loops.

Every action should bind actor, authority, target, evidence, precondition, postcondition, artifact, and outcome when practical.
